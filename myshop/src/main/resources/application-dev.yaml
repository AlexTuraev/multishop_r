server:
  port: 8180

spring:
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://localhost:8085/realms/master
        registration:
          myshop:
            provider: keycloak
            authorization-grant-type: client_credentials
            client-id: myshop
            client-secret: HMN0hwa4rx6taRdBbdKzTS6ekh88IiWs
            scope: profile
            client-authentication-method: client_secret_post
  #postgres
  datasource:
#    url: jdbc:postgresql://localhost:5432/myshop
#    username: admin
#    password: admin
    driver-class-name: org.postgresql.Driver
#    dbcp2:
#      driver-class-name: org.postgresql.Driver
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/myshop
    username: admin
    password: admin
    pool:
      enabled: true
      initial-size: 10
      max-size: 50
      validation-query: SELECT 1
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    redis:
      time-to-live: PT2M
#  jpa:
#    properties:
#      hibernate:
#        cache:
#          use_second_level_cache: true
#          region:
#            factory_class: org.hibernate.cache.jcache.JCacheRegionFactory
#    show-sql: true

app:
  payment:
    url: http://localhost:8280/payment/userbalance